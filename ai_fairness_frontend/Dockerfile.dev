# 1. 使用不带后缀的“完全版” Node.js 镜像
FROM node:18

# 设置容器内的工作目录
WORKDIR /app

# 复制 package.json 和 lock 文件
COPY package*.json ./

# 2. 使用 npm ci 进行更可靠的纯净安装
RUN npm ci

# 暴露 Vite 默认的开发端口 5173
EXPOSE 5173

# 设置容器启动时要执行的默认命令
CMD ["npm", "run", "dev"]